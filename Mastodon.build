#! /bin/sh
#
# Mastodon build for savelogs
#

. ./Mastodon.inc
exedir ROOT

test Makefile && make distclean 

CC="$SYSCC" CFLAGS="-O2" ./configure.sh --prefix=/usr --confdir=/etc
make || exit 1
ROOT=$ROOT make install install.conf
exit 0
